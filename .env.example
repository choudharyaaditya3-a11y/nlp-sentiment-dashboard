# Optional. Only needed for the executive-summary feature in src/summary.py
# (a single templated Claude API call that phrases already-computed numbers
# in plain English). The rest of the dashboard works fully without this.
#
# Get a key at https://console.anthropic.com/ and copy this file to .env
# (never commit the real .env file - it's gitignored).
ANTHROPIC_API_KEY=
